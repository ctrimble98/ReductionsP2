#include "reductions.h"

void parseCNF() {

    // istream* reader = cin;

    //Read line until start with p -> read chars no whitespace or \n until 0 for each clause

    char c;
    while (std::cin >> c) {

        std::cout << c;
    }
}
